#include <string.h>

size_t ft_strlcat(char *dst, const char *src, size_t size)
{
	size_t	i;

	i = 0;

}

#include <stdio.h>
int	main(int ac, char **av)
{
	//printf("%lu\n", strlcat(av[1], av[2], 2));
		printf("%d\n", count_char(av[1], 'c'));

	return (0);
}